name: Python CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: ‚¨áÔ∏è Checkout code
        uses: actions/checkout@v4

      - name: Ì∞ç Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Ì≥¶ Install dependencies (if any)
        run: |
          python -m pip install --upgrade pip
          # pip install -r requirements.txt  # Uncomment if you add dependencies

      - name: Ì∫Ä Run hello.py
        run: python hello.py
